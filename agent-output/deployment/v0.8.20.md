---
Status: Active
Version: v0.8.20
PreparedAtUTC: 2026-03-23T17:07Z
Branch: release/v0.8.20-prep
---

# Deployment Readiness — v0.8.20

## Summary

- Release type: Patch (hotfix)
- Goal: Ship MuslimBusiness importer fix for client-rendered directory pages
- Base: `origin/main` @ `718efb5`
- Fix commit (cherry-picked onto release branch): `13e811e`
- Versioning artifacts: `package.json`/`package-lock.json` bumped to `0.8.20` (not yet committed)

## Included Changes

- MuslimBusiness importer fallback acquisition for client-rendered source pages

## Pre-Release Verification

### Version Consistency

- Latest tag on repo: `v0.8.19`
- `origin/main:package.json` version: `0.8.19`
- `release/v0.8.20-prep:package.json` version (working tree): `0.8.20`
- Changelog entry added: `CHANGELOG.md` → `0.8.20` dated `2026-03-23`

### Git State

- Branch: `release/v0.8.20-prep` (tracking `origin/main`)
- Ahead/behind: ahead by 1 commit (fix), plus local versioning edits not committed
- Working tree changes:
  - `CHANGELOG.md`
  - `package.json`
  - `package-lock.json`

### Security / Audit

- `npm audit`: **0 vulnerabilities**

### Tests

- Scoped MuslimBusiness verification (passed):
  - `src/__tests__/utils/muslimbusiness-parser.test.ts`
  - `src/__tests__/utils/muslimbusiness-client-dataset.test.ts`
  - `src/__tests__/scripts/import-muslimbusiness-cli.test.ts`

- Full test suite status (BLOCKER on main, not introduced by this release work):
  - `npx vitest run` fails on `src/__tests__/components/AdminProvidersPageContent.test.tsx`
  - Failure reproduces on `origin/main` at `718efb5`

### Build / Packaging

- `npm run build:standalone`:
  - Fails when `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` are unset (expected due to env validation in `src/lib/supabase/client.ts`).
  - Succeeds when those env vars are provided (validated locally using safe placeholder values).

## Risks / Notes

- **Known failing test on `origin/main`** blocks a clean “all tests green” release unless the team accepts shipping with this pre-existing failure.
- Build requires `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY` to be present in CI/build environments.

## Go/No-Go Recommendation

- **No-Go** for a strict release gate requiring `vitest` full-suite pass.
- **Conditional Go** if you explicitly accept releasing while `origin/main` has the known failing `AdminProvidersPageContent` test (scoped importer tests + type-check + audit are green).

## Next Actions (Pending User Approval)

1. Commit version bump + changelog (`0.8.20`) on `release/v0.8.20-prep`
2. Push branch and open PR to `main`
3. Tag `v0.8.20` on the version-bump commit and push tag

