---
ID: 063
Origin: 063
UUID: b7e3a1d9
Status: Released
---

# Release Deployment: v0.9.3

| Field | Value |
|-------|-------|
| Version | v0.9.3 |
| Release Type | Standalone patch |
| Included Plans | 063 |
| Environment | GitHub branch push + tag |
| Release Status | Released |
| Stage 2 Started | 2026-03-25T21:52Z |

## Release Summary

Plan 063 is released as `v0.9.3`. After `.env.local` became available, the production build passed, the branch was pushed, tag `v0.9.3` was created, and local smoke checks confirmed `/` and `/providers` render correctly from the built app.

## Changelog

| Date (UTC) | Agent | Change |
|------------|-------|--------|
| 2026-03-25T21:52Z | DevOps | Stage 2 initiated after user approval. Readiness checks run: branch rebased onto `origin/main`, audit performed, build attempted with template env values. Release blocked before push because build requires a real `NEXT_PUBLIC_SUPABASE_ANON_KEY`. |
| 2026-03-25T22:05Z | DevOps | Version collision correction. Upstream advanced to released `v0.9.2`, so the pending Plan 063 release was renumbered to `v0.9.3`. Stage 2 remains blocked pending real build credentials. |
| 2026-03-25T22:25Z | DevOps | Stage 2 completed. Real-env build passed, branch `session/063-safe-area-top-gap` pushed, compare URL confirmed conflict-free by ancestry check, tag `v0.9.3` created and pushed, and local smoke checks for `/` and `/providers` passed. |

## Evidence

### Branch Tracking / Sync

- Session branch rebased onto `origin/main` before push
- Ahead/behind before release push: `0 behind / 1 ahead`
- Branch push: `git push origin session/063-safe-area-top-gap` ✅
- Compare URL: `https://github.com/abu-lina/uflow/compare/main...session/063-safe-area-top-gap`
- Conflict-free confirmation: `git merge-base --is-ancestor origin/main HEAD` returned success before push
- Tag push: `git push origin v0.9.3` ✅

### Security Audit

- `npm audit --audit-level=high` returned 1 HIGH and 1 MODERATE vulnerability
- HIGH: `picomatch` ReDoS / method-injection advisory chain
- Assessment: not introduced by Plan 063 or Stage 1/2 release work; the dependency references in `package-lock.json` are unchanged across `HEAD^` and `HEAD` except the root version metadata
- Blocker status: not a new release-introduced HIGH, therefore not the blocking issue for this release

### Packaging Validation

- `npx tsc --noEmit`: PASS (QA evidence)
- `npx vitest run src/__tests__/components/MobileProviderDetail.safe-area.test.tsx --reporter=verbose`: PASS (QA evidence)
- `npx vitest run`: PASS (QA evidence)
- `npm run build` with real `.env.local`: PASS (`EXIT:0`) ✅

### Functional Smoke Tests

- Local server started from built app on `http://127.0.0.1:3010` using `PORT=3010 npm run start`
- Root route check:
  - `curl -I -s http://127.0.0.1:3010/ | head -n 5` returned `HTTP/1.1 200 OK`
  - HTML contains the primary search UI shell and `viewport-fit=cover`
- Providers route check:
  - `curl -sS -D - http://127.0.0.1:3010/providers -o /tmp/uflow-providers-smoke.html`
  - rendered HTML contains provider search UI and initial provider result payloads
  - no immediate `No results found` empty-state was detected

## Deferred / Open Actions

See `agent-output/planning/063-open-actions.md`.

Current open items still relevant:
- manual iOS notch / non-notch screenshot validation within 24h of production release

## Next Actions

1. Complete the deferred physical-device iOS validation in `agent-output/planning/063-open-actions.md`.
2. Hand off to Roadmap / Retrospective for release tracking and follow-up closure.
