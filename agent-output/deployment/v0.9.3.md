---
ID: 063
Origin: 063
UUID: b7e3a1d9
Status: Released
---

# Release Deployment: v0.9.3

| Field | Value |
|-------|-------|
| Version | v0.9.3 |
| Release Type | Standalone patch |
| Included Plans | 063 |
| Environment | GitHub branch push + tag + UAT workflow deploy |
| Release Status | Released |
| Stage 2 Started | 2026-03-25T21:52Z |

## Release Summary

Plan 063 is released as `v0.9.3` and deployed to UAT. After `.env.local` became available, the production build passed, the branch was pushed, tag `v0.9.3` was created, and local smoke checks confirmed `/` and `/providers` render correctly from the built app. On 2026-03-26, the branch was promoted to `https://uat.ummahflow.com` through `deploy-uat.yml` after fixing a Hetzner low-disk failure in the UAT workflow.

## Changelog

| Date (UTC) | Agent | Change |
|------------|-------|--------|
| 2026-03-25T21:52Z | DevOps | Stage 2 initiated after user approval. Readiness checks run: branch rebased onto `origin/main`, audit performed, build attempted with template env values. Release blocked before push because build requires a real `NEXT_PUBLIC_SUPABASE_ANON_KEY`. |
| 2026-03-25T22:05Z | DevOps | Version collision correction. Upstream advanced to released `v0.9.2`, so the pending Plan 063 release was renumbered to `v0.9.3`. Stage 2 remains blocked pending real build credentials. |
| 2026-03-25T22:25Z | DevOps | Stage 2 completed. Real-env build passed, branch `session/063-safe-area-top-gap` pushed, compare URL confirmed conflict-free by ancestry check, tag `v0.9.3` created and pushed, and local smoke checks for `/` and `/providers` passed. |
| 2026-03-26T08:52Z | DevOps | First UAT deployment attempt (`deploy-uat.yml` run `23585376852`) failed on Hetzner while pulling `ghcr.io/abu-lina/uflow-uat:latest` with `no space left on device`. |
| 2026-03-26T09:00Z | DevOps | Second UAT deployment attempt (`deploy-uat.yml` run `23585871258`) failed with the same low-disk pull error after adding pre-pull Docker cleanup in commit `4edabf1c`. |
| 2026-03-26T09:14Z | DevOps | Third UAT deployment attempt (`deploy-uat.yml` run `23586320529`) succeeded after adding a fallback to stop the current UAT container, prune aggressively, and retry the pull in commit `f25edde0`. Live UAT smoke checks for `/api/health` and `/providers` returned HTTP 200. |
| 2026-03-28T12:19Z | DevOps | UAT redeployed from `session/063-safe-area-top-gap` via `workflow_dispatch` (`deploy-uat.yml` run `23684908748`) at SHA `ba39402c`. UAT smoke checks for `/api/health` and `/providers` returned HTTP 200. |

## Evidence

### Branch Tracking / Sync

- Session branch rebased onto `origin/main` before push
- Ahead/behind before release push: `0 behind / 1 ahead`
- Branch push: `git push origin session/063-safe-area-top-gap` ✅
- Compare URL: `https://github.com/abu-lina/uflow/compare/main...session/063-safe-area-top-gap`
- Conflict-free confirmation: `git merge-base --is-ancestor origin/main HEAD` returned success before push
- Tag push: `git push origin v0.9.3` ✅

### UAT Deployment

- Workflow: `deploy-uat.yml`
- Successful runs:
  - `23586320529` (2026-03-26) on branch `session/063-safe-area-top-gap` at SHA `f25edde0a0953bef68847ae18f501b016af1fd3a`
  - `23684908748` (2026-03-28) on branch `session/063-safe-area-top-gap` at SHA `ba39402ce6f6733f643b529aabc8b560ad368d59`
- Run URLs:
  - `https://github.com/abu-lina/uflow/actions/runs/23586320529`
  - `https://github.com/abu-lina/uflow/actions/runs/23684908748`
- Earlier failed UAT runs captured for auditability:
  - `23585376852` failed during `docker pull` on Hetzner with `no space left on device`
  - `23585871258` failed with the same disk-pressure error after initial cleanup hardening
- Workflow hardening delivered during UAT promotion:
  - commit `4edabf1c` adds pre-pull Docker cleanup on the Hetzner host
  - commit `f25edde0` adds a low-disk fallback that stops the current UAT container, prunes Docker state, and retries the image pull once

### Security Audit

- `npm audit --audit-level=high` returned 1 HIGH and 1 MODERATE vulnerability
- HIGH: `picomatch` ReDoS / method-injection advisory chain
- Assessment: not introduced by Plan 063 or Stage 1/2 release work; the dependency references in `package-lock.json` are unchanged across `HEAD^` and `HEAD` except the root version metadata
- Blocker status: not a new release-introduced HIGH, therefore not the blocking issue for this release

### Packaging Validation

- `npx tsc --noEmit`: PASS (QA evidence)
- `npx vitest run src/__tests__/components/MobileProviderDetail.safe-area.test.tsx --reporter=verbose`: PASS (QA evidence)
- `npx vitest run`: PASS (QA evidence)
- `npm run build` with real `.env.local`: PASS (`EXIT:0`) ✅

### Functional Smoke Tests

- Local server started from built app on `http://127.0.0.1:3010` using `PORT=3010 npm run start`
- Root route check:
  - `curl -I -s http://127.0.0.1:3010/ | head -n 5` returned `HTTP/1.1 200 OK`
  - HTML contains the primary search UI shell and `viewport-fit=cover`
- Providers route check:
  - `curl -sS -D - http://127.0.0.1:3010/providers -o /tmp/uflow-providers-smoke.html`
  - rendered HTML contains provider search UI and initial provider result payloads
  - no immediate `No results found` empty-state was detected

### UAT Smoke Tests

- `curl -I -s https://uat.ummahflow.com/api/health | head -n 10` returned `HTTP/2 200` at `2026-03-26T09:22Z`
- `curl -I -s https://uat.ummahflow.com/providers | head -n 10` returned `HTTP/2 200` at `2026-03-26T09:22Z`
- Conclusion: UAT is serving the newly deployed branch successfully at the domain level

## Deferred / Open Actions

See `agent-output/planning/063-open-actions.md`.

Current open items still relevant:
- manual iOS notch / non-notch screenshot validation within 24h of production release

## Next Actions

1. Complete the deferred physical-device iOS validation in `agent-output/planning/063-open-actions.md`, now against the live UAT environment.
2. Hand off to Roadmap / Retrospective for release tracking and follow-up closure.
