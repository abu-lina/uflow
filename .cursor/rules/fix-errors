I'm building a Next.js 14 app using the App Router, Tailwind CSS, and Supabase for backend (auth + database), hosted on Vercel. Here's a summary of the setup:

- Tailwind CSS for styling
- Supabase with environment variables (Supabase URL, anon/service role keys, auth redirects, email template ID)
- Environment has feature flags, security headers, and performance configs (image optimization, cache control)
- App name: Ummah Flow
- Local and production URLs are handled via NEXT_PUBLIC_SITE_URL

Please do the following:

1. **Fix all ESLint, TypeScript, and build errors** across the app.
2. **Remove unused variables, imports, and types**. Refactor redundant logic.
3. **Check and validate Supabase integration**, especially around auth, redirects, and role-based access.
4. **Ensure Tailwind CSS usage follows best practices** (avoid unnecessary classes, duplicate styles, or poor responsive usage).
5. **Verify and optimize next.config.js**, middleware, and route handlers for production readiness.
6. Ensure `.env` variables are referenced correctly throughout the codebase.
7. **Confirm that the app is fully ready for `npm run build` and Vercel deployment.**
8. Optimize performance where possible (loading strategies, SEO meta tags, proper use of dynamic vs static rendering).
9. Confirm that security headers (CSP and HSTS) are applied properly.
10. Suggest or apply improvements for better structure, DX (developer experience), and long-term maintainability.

Let me know if anything is misconfigured, missing, or can be improved further for a production-ready state.
