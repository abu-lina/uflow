name: Deploy to Hetzner

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  NODE_VERSION: '20'

jobs:
  deploy:
    name: Build & Deploy
    runs-on: ubuntu-latest
    timeout-minutes: 15
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Build Docker image
        run: |
          docker build -t uflow:latest \
            --build-arg NEXT_PUBLIC_SUPABASE_URL="${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}" \
            --build-arg NEXT_PUBLIC_SUPABASE_ANON_KEY="${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}" \
            --build-arg NEXT_PUBLIC_SITE_URL="https://ummahflow.com" \
            --platform linux/amd64 .
      
      - name: Save Docker image
        run: docker save uflow:latest | gzip > uflow.tar.gz
      
      - name: Upload files to Hetzner
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.HETZNER_HOST }}
          username: root
          key: ${{ secrets.HETZNER_SSH_KEY }}
          source: "uflow.tar.gz,nginx-template.conf"
          target: "/tmp/"
      
      - name: Deploy to Hetzner
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HETZNER_HOST }}
          username: root
          key: ${{ secrets.HETZNER_SSH_KEY }}
          script: |
            set -e
            cd /tmp
            
            echo "🛑 Stopping old container..."
            docker stop uflow-app || true
            docker rm uflow-app || true
            
            echo "📦 Loading new image..."
            docker load < uflow.tar.gz
            
            echo "🚀 Starting new container..."
            docker run -d -p 3000:3000 \
              -e NEXT_PUBLIC_SUPABASE_URL="${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}" \
              -e NEXT_PUBLIC_SUPABASE_ANON_KEY="${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}" \
              -e SUPABASE_SERVICE_ROLE_KEY="${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
              -e RESEND_API_KEY="${{ secrets.RESEND_API_KEY }}" \
              -e NEXT_PUBLIC_SITE_URL="https://ummahflow.com" \
              --name uflow-app uflow:latest
            
            echo "⏳ Waiting for app to start..."
            sleep 10
            
            echo "🏥 Health check..."
            for i in {1..30}; do
                if curl -f http://localhost:3000/api/health > /dev/null 2>&1; then
                    echo "✅ App is healthy"
                    break
                fi
                [ $i -eq 30 ] && echo "❌ Health check failed" && docker logs --tail 50 uflow-app && exit 1
                sleep 2
            done
            
            echo "🔧 Updating Nginx..."
            sed "s/{{DOMAIN}}/ummahflow.com/g" /tmp/nginx-template.conf > /etc/nginx/sites-available/ummahflow
            
            echo "🧪 Testing Nginx config..."
            if nginx -t; then
                systemctl reload nginx
                echo "✅ Nginx reloaded"
            else
                echo "❌ Nginx config invalid"
                exit 1
            fi
            
            echo "🌐 Verifying deployment..."
            sleep 3
            if curl -f https://ummahflow.com/api/health > /dev/null 2>&1; then
                echo "✅ Deployment successful!"
            else
                echo "⚠️ Health check through domain failed (may need DNS propagation)"
            fi
            
            rm -f /tmp/uflow.tar.gz
            echo "🎉 Deployment complete!"
      
      - name: Deployment summary
        if: success()
        run: |
          echo "## ✅ Deployment Successful!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Live URL:** https://ummahflow.com" >> $GITHUB_STEP_SUMMARY
          echo "**Health Check:** https://ummahflow.com/api/health" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "🚀 Your app is now live!" >> $GITHUB_STEP_SUMMARY
