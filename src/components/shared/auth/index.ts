/**
 * Shared Auth Components
 * 
 * Reusable authentication-related components that can be used across the application.
 * These components handle the UI aspects of authentication while the business logic
 * is managed by the auth feature.
 */

import LoginForm from './login-form';
import { SignupForm } from './SignupForm';

export { LoginForm, SignupForm }; 