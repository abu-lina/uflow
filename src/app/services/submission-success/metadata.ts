import { generateServiceMetadata } from '@/config/metadata';

// Create service-specific metadata for the submission success page
export default generateServiceMetadata(
  'Submission Successful',
  'Your service has been submitted successfully and is pending review.'
); 